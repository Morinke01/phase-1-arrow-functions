//arrow functions
//Dividing two numbers
const divide = (num1, num2) => {
    num1=2000;
    num2=100;
    return num1/num2;
}
divide;
//multiplying a number by itself
const square = x => (x ** 2);
square;
// array function to add two numbers
const add = (parameter1, parameter2) => parameter1 + parameter2;
add;
